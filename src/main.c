#include "prime.h"
#include <alloca.h>
#include <string.h>
#include <assert.h>

int main(void) {
  seed();
  BN_init(a)
  RNG_PARAMS *params = bn_gen_bits_params(64);
  bn_gen_bits(a, 64, params);
  BN_hprintln(a);
  return 0;
}
